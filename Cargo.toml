[package]
name = "chromawave"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
license = "Apache-2.0"
description = "Palette extraction, mixing, and contrast checks from the CLI"

[dependencies]
